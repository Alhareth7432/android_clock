name: clock
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

ddependencies:
  flutter:
    sdk: flutter
    
  home_widget: ^0.6.0

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true